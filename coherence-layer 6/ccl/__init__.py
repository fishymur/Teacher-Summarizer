"""Curriculum Coherence Layer - Milestone 1: Curriculum Contract foundation.

This package implements the executable-pedagogy core that precedes the tutor
runtime: the multi-tenant data foundation, materials with source anchors, the
versioned Curriculum Contract, its publish-gate validator, the deterministic
policy engine, and an append-only audit log.

Deliberately out of scope for this milestone (belongs to the tutor runtime and
later phases): response generation, verification, citation rendering, retrieval
embeddings, the insight pipeline, and the web UI.
"""

__version__ = "0.1.0"
